name: 'online_retail'
version: '1.0.0'
config-version: 2

profile: 'online_retail'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  online_retail:
    staging:
      +materialized: view
    marts:
      +materialized: table
