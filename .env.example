# Copier ce fichier en .env et ajuster si besoin.
# Sans DATABASE_URL défini, le pipeline utilise un fichier DuckDB local
# (warehouse.duckdb à la racine du repo) - aucun service externe requis
# pour développer (voir common/config.py).

# DATABASE_URL=duckdb:///warehouse.duckdb
