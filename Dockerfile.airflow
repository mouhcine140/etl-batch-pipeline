# Image Airflow + dépendances du repo (extract/load/dbt), pour que les
# tâches du DAG puissent importer les modules du projet et lancer dbt.
FROM apache/airflow:2.10.5-python3.11

COPY requirements.txt /requirements.txt
RUN pip install --no-cache-dir -r /requirements.txt
